buildTypes {
    release {
        minifyEnabled true
        shrinkResources false 
        zipAlignEnabled true // Дополнительное выравнивание для стабильности
        proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
    }
}
