buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        // Стабильная версия плагина, совместимая с Java 17
        classpath 'com.android.tools.build:gradle:8.2.2'
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}
